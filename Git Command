git command:

git commit -m "version1
git status
git init
git add index.txt (or) git add .
git config --global user.email "you@example.com"
git config --global user.name "Your Name"
git commit -m "version1"
git log


Github command:


git remote add origin git@github.com:shahabdafedar/Git-Practice.git
git push -u origin master
ssh-keygen -t rsa -b 4096 -C "shahab@gmail.com"
eval $(ssh-agent -s)
ssh-add ~/.ssh/id_rsa
clip < ~/.ssh/id_rsa.pub
git push -u origin master
git pull origin master


git diff index.txt
git diff --staged index.txt             'to get difference before commit

git reset index.txt       ' to get back from stage before doing commit

git checkout .         'to undo all changes from all file which is there in stage state before doing commit